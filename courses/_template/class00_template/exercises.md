# Exercises — Course X · ch Y · class Z

Three exercises. Keep the structure: warm-up → apply → stretch. Each should be runnable from the same `train.py` with config overrides; do not introduce a parallel script.

## 1. Warm-up — <small variation>

<5–10 min. The student changes one config field and re-runs. Goal: build muscle memory for the run loop.>

## 2. Apply — <real comparison>

<10–20 min. The student runs the lesson against a different backbone (or dataset slice) and tabulates the result. Goal: see the lesson's effect across the matrix.>

## 3. Stretch — <conceptual extension>

<Open-ended. The student modifies the lesson — either by adding a new metric, swapping a key component, or implementing one of the open questions from the README's Research section. Goal: cross from "follow recipe" to "extend recipe".>
